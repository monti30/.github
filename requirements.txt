# Python 3.10+
# Install PyTorch for your platform first if the default wheel lacks CUDA/MPS support:
# https://pytorch.org/get-started/locally/

torch>=2.0
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
tqdm>=4.66
